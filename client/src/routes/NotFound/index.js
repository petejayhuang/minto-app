import React from 'react'

const NotFound = props => (
  <div className="route-container pl-3 pr-3">Could not find that route!</div>
)

NotFound.defaultProps = {}
NotFound.propTypes = {}

export default NotFound
